// Firebase is not configured. To enable authentication and database features,
// provide your Firebase config in the environment variables.
export const app = null;
export const auth = null;
export const db = null;
